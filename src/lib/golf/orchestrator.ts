import { MatchSnapshot, HoleOutcome } from './types';
import { evaluateThirtyTenTen } from './thirtyTenTen';

/**
 * Thin wrapper for now; will expand when wiring UI.
 */
export function makeSnapshot(holes: number[], holeOutcomes: HoleOutcome[]): MatchSnapshot {
  throw new Error('not implemented');
}
